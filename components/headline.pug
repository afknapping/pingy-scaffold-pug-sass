mixin headline(text, level)

	if !text
		- var text = 'text missing'
	if !level
		- var level = 1

	case level
		when 1
			h1= text
		when 2
			h2= text
		when 3
			h3= text
		when 4
			h4= text
		when 5
			h5= text
		when 6
			h6= text

	style
		:sass
			h1, h2, h3, h4, h5, h6
				margin-top: 0